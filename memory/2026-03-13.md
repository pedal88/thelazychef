Refactored style center to be fully dynamic, supporting ingredients, recipes, and taxonomies with CRUD capabilities for columns in a React-like visual test matrix UI.
- Added a Gemini-powered Auto-Generate Prompt feature to the Add Style preset modal, which suggests keywords based on the scope and provided preset name.
- Added draggable ordering attributes using HTML5 Drag/Drop API to Style Sandbox columns, updating PostgreSQL order_index property via /req/reorder-presets back-end.
