REFERENCE VOCABULARIES (Use these exact terms for classification):
- Valid Diets: {{ vocab.diets | join(', ') }}
- Valid Cuisines: {{ vocab.cuisines | join(', ') }}
- Valid Meal Types: {{ vocab.meal_types | join(', ') }}
- Valid Cooking Methods: {{ vocab.cooking_methods | join(', ') }}
- Valid Tastes: {{ vocab.tastes | join(', ') }}
- Valid Time Intervals: {{ vocab.time_intervals | join(', ') }}
- Valid Cleanup Tags: {{ vocab.cleanup_factors | join(', ') }}
- Valid Difficulties: {{ vocab.difficulty | join(', ') }}
